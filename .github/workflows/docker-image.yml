
on:
  push:
    branches: 
      - main
      - staging
    
  #pull_request:
    #branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Build the Docker image
      uses: mr-smithers-excellent/docker-build-push@v5
      with:
        image: ghcr.io/sohag033/vaultwarden
        registry: ghcr.io
        username: sohag033
        password: Samajpati044
        tags: ${{ github.sha }}

